apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "visualization.fullname" . }}-test-connection"
  labels:
    {{- include "visualization.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "visualization.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
